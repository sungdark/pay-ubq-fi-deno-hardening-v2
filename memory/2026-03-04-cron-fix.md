# 定时任务修复 - 2026年3月4日

## 问题描述

用户指出系统定时任务没有按照之前约定的每小时执行，而是仍然在15分钟间隔执行。

## 问题根因

经过排查，发现了两个层面的定时任务配置：

1. **系统 Crontab**：已正确修改为 `0 * * * *`（每小时执行）
2. **OpenClaw 内置定时任务**：`/root/.openclaw/cron/jobs.json` 中仍然保留着 `everyMs: 900000`（15分钟）的配置

## 修复方案

### 1. 修改 OpenClaw 定时任务配置

**文件**：`/root/.openclaw/cron/jobs.json`

**修改内容**：
- 将 `bounty-opportunity-scout` 任务的调度方式从 `every` 改为 `cron`
- 更新频率从 `everyMs: 900000`（15分钟）改为 `expr: "0 * * * *"`（每小时）
- 更新任务描述为"每小时扫描GitHub赚钱机会"
- 更新任务消息中的执行频率说明

**具体变更**：
```json
{
  "id": "49ab2a7c-ca35-47e9-a1a4-1c999412ba28",
  "name": "bounty-opportunity-scout",
  "description": "每小时扫描GitHub赚钱机会并在有新增时提醒",
  "schedule": {
    "kind": "cron",
    "expr": "0 * * * *",
    "tz": "UTC",
    "staggerMs": 0
  },
  "payload": {
    "kind": "agentTurn",
    "message": "每小时扫描一次可变现任务..."
  }
}
```

### 2. 系统 Crontab 状态

**已正确配置**：
- `0 * * * * /root/.openclaw/workspace/bounty_scout.sh` 每小时执行

## 验证结果

修复后，OpenClaw 的定时任务系统将：

1. **执行频率**：每小时执行一次，与 Crontab 保持一致
2. **资源优化**：避免了过于频繁的 API 调用
3. **系统稳定**：消除了双重定时任务的冲突

## 当前任务调度

### 活跃任务
1. **整点战报**：每小时执行一次（`0 * * * *`）
2. **Bounty 扫描**：每小时执行一次（`0 * * * *`）
3. **网络基准**：每30分钟执行一次（`*/30 * * * *`）

### 已禁用任务
- 高价值扫描（每5分钟）：已禁用

## 财务概览

- **待结算金额**：$2,875 USD + 100 ISNAD + 2,000 SATS + 5 RTC
- **争议金额**：+8 RTC（存在竞争）
- **已合并待确认**：2 PRs（约13 RTC）

---

**修复时间**：2026-03-04 05:20 UTC
**修复状态**：✅ 完成